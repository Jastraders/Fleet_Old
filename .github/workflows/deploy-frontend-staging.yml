name: CI / Deploy Frontend Staging (Firebase Hosting)

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        working-directory: packages/fleet-front

      - name: Build staging frontend
        run: npm run build:staging
        working-directory: packages/fleet-front

      - name: Deploy to Firebase Hosting Staging
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          FIREBASE_PROJECT: ${{ secrets.GCP_PROJECT }}
        run: |
          npx firebase-tools deploy --only hosting:staging --project "$FIREBASE_PROJECT" --token "$FIREBASE_TOKEN"
        working-directory: packages/fleet-front
