services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: fleet-backend
    ports:
      - "3001:3001"
    env_file:
      - packages/fleet-back/.env
    volumes:
      - ./packages/fleet-back:/app/packages/fleet-back
