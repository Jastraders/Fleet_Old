{
  "name": "fleet-back",
  "type": "module",
  "scripts": {
    "dev": "bun run --port 3001 --watch src/index.ts",
    "db:generate": "drizzle-kit generate --name",
    "db:generate:custom": "drizzle-kit generate --custom --name",
    "db:studio": "drizzle-kit studio"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts"
    }
  },
  "dependencies": {
    "@orpc/server": "^1.13.4",
    "drizzle-kit": "^0.31.8",
    "drizzle-orm": "^0.45.0",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv": "^16.0.0",
    "typescript": "^5.7.2"
  }
}
